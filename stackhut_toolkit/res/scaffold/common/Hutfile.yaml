# Service name (use lower case letters, numbers, and dashes)
name: {{ scaffold.name }}

# Service description
description: Awesome service in {{ scaffold.stack.name }}

# GitHub source repo link (optional)
# github_url: www.github.com/...

# The service dependencies, in terms of the base OS and language runtime
baseos: {{ scaffold.baseos.name }}
stack: {{ scaffold.stack.name }}

# Persist the service between requests
persistent: True

# Restrict service access to authenticated users
private: True
